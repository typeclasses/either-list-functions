name: either-list-functions
version: 0.0.0.2
github: chris-martin/either-list-functions

synopsis: Functions involving lists of Either

description: |
  Functions involving lists of Either

category: Data

author:     Chris Martin <ch.martin@gmail.com>
maintainer: Chris Martin <ch.martin@gmail.com>

license: Apache-2.0
license-file: license.txt

ghc-options:
  - -Wall

library:
  source-dirs: src
  exposed-modules:
    - Data.List.EitherFunctions

dependencies:
  - base >= 4.9 && < 4.12

tests:

  doctest:
    source-dirs: test
    main: doctest.hs
    ghc-options:
      - -threaded
    dependencies:
      - either-list-functions
      - doctest
